<UserControl x:Class="RestaurantWPF.Views.Customer.Components.FeedbackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:RestaurantWPF.Converters"
             mc:Ignorable="d"
             FontFamily="Segoe UI"
             Background="#121212">

    <UserControl.Resources>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <local:RatingToColorConverter x:Key="RatingToColorConverter"/>

        <SolidColorBrush x:Key="AccentBrush" Color="#FF7A00"/>
        <SolidColorBrush x:Key="CardBackground" Color="#25221F"/>
        <SolidColorBrush x:Key="InputBackground" Color="#3B3631"/>
        <SolidColorBrush x:Key="InputBackgroundHover" Color="#453F3A"/>
        <SolidColorBrush x:Key="BorderLight" Color="#544E49"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#CFCFCF"/>
        <SolidColorBrush x:Key="BorderDark" Color="#2E2E2E"/>
        <SolidColorBrush x:Key="RowHoverBackground" Color="#2E2A26"/>

        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="20"
                          ShadowDepth="2"
                          Opacity="0.25"
                          Color="Black"/>

        <Style x:Key="StarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Path Data="M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.63L12,2L9.19,8.63L2,9.24L7.46,13.97L5.82,21L12,17.27Z"
                              Fill="{TemplateBinding Tag}"
                              Width="32" Height="32"
                              Stretch="Uniform"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputBaseStyle" TargetType="Control">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <AdornerDecorator>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Padding="{TemplateBinding Padding}"
                                              Focusable="False"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Hidden"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </AdornerDecorator>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource InputBackgroundHover}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Color="#FF7A00" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource InputBaseStyle}">
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6,6,0,0"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter TargetName="TabBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF7A00" BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <TabControl Background="{StaticResource CardBackground}"
                    BorderThickness="0"
                    FontSize="15"
                    Foreground="White"
                    Padding="10"
                    Effect="{StaticResource CardShadow}">

            <TabItem Header="Đánh giá món ăn">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Món ăn đã thưởng thức"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource AccentBrush}"
                                       Margin="0,0,0,15"
                                       Grid.Row="0"/>

                            <ScrollViewer Grid.Row="1"
                                          VerticalScrollBarVisibility="Auto"
                                          MaxHeight="600">
                                <ItemsControl ItemsSource="{Binding CompletedFoods}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource InputBackground}"
                                                    CornerRadius="10"
                                                    Margin="0,0,10,12"
                                                    Padding="10"
                                                    BorderBrush="{StaticResource BorderLight}"
                                                    BorderThickness="1"
                                                    Cursor="Hand"
                                                    Effect="{StaticResource CardShadow}">
                                                <DockPanel MouseLeftButtonUp="OnFoodSelected">
                                                    <Border Width="70" Height="70"
                                                            Margin="0,0,10,0"
                                                            DockPanel.Dock="Left"
                                                            CornerRadius="6"
                                                            ClipToBounds="True">
                                                        <Image Source="{Binding ImageUrl}"
                                                               Stretch="UniformToFill"
                                                               VerticalAlignment="Center"
                                                               HorizontalAlignment="Center"/>
                                                    </Border>

                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Name}"
                                                                   FontWeight="Bold"
                                                                   FontSize="14"
                                                                   Foreground="{StaticResource TextPrimary}"/>
                                                        <TextBlock Text="{Binding Price, StringFormat='Giá: {0:N0}đ'}"
                                                                   Foreground="{StaticResource AccentBrush}"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="12"/>
                                                        <TextBlock Text="Bấm để đánh giá"
                                                                   Foreground="{StaticResource TextSecondary}"
                                                                   FontStyle="Italic"
                                                                   FontSize="11"/>
                                                    </StackPanel>
                                                </DockPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>

                        <Border Grid.Column="1"
                                Background="{StaticResource CardBackground}"
                                CornerRadius="14"
                                BorderBrush="{StaticResource BorderDark}"
                                BorderThickness="1"
                                Padding="30"
                                Effect="{StaticResource CardShadow}"
                                Margin="20,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Đánh giá món ăn"
                                           FontSize="22"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource AccentBrush}"
                                           Margin="0,0,0,20"/>

                                <Border Background="{StaticResource InputBackground}"
                                        BorderBrush="{StaticResource AccentBrush}"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Padding="10"
                                        Margin="0,0,0,20"
                                        Visibility="{Binding SelectedFood, Converter={StaticResource NullToVisibilityConverter}}">
                                    <DockPanel>
                                        <Border Width="100" Height="100"
                                                CornerRadius="8"
                                                DockPanel.Dock="Left"
                                                ClipToBounds="True"
                                                Margin="0,0,15,0">
                                            <Image Source="{Binding SelectedFood.ImageUrl}"
                                                   Stretch="UniformToFill"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"/>
                                        </Border>

                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding SelectedFood.Name}"
                                                       FontSize="15"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource TextPrimary}"/>
                                            <TextBlock Text="{Binding SelectedFood.Price, StringFormat='Giá: {0:N0}đ'}"
                                                       Margin="0,5,0,0"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource AccentBrush}"/>
                                        </StackPanel>
                                    </DockPanel>
                                </Border>

                                <TextBlock Text="Đánh giá:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"/>
                                <ItemsControl ItemsSource="{Binding Ratings}" Margin="0,8,0,20">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Style="{StaticResource StarButtonStyle}"
                                                    Command="{Binding DataContext.SelectRatingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}">
                                                <Button.Tag>
                                                    <MultiBinding Converter="{StaticResource RatingToColorConverter}">
                                                        <Binding Path="DataContext.SelectedRating" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                        <Binding/>
                                                    </MultiBinding>
                                                </Button.Tag>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <TextBlock Text="Nhận xét:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"/>

                                <TextBox Text="{Binding Comment, Mode=TwoWay}"
                                         Style="{StaticResource {x:Type TextBox}}"
                                         AcceptsReturn="True"
                                         Height="100"
                                         TextWrapping="Wrap"
                                         Padding="10"
                                         VerticalContentAlignment="Top"
                                         Margin="0,5,0,20"/>

                                <Button Content="Gửi đánh giá"
                                        Command="{Binding SubmitFeedbackCommand}"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Width="160"
                                        HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Lịch sử đánh giá">
                <Border Margin="20"
                        Background="{StaticResource CardBackground}"
                        CornerRadius="14"
                        BorderBrush="{StaticResource BorderDark}"
                        BorderThickness="1"
                        Padding="20"
                        Effect="{StaticResource CardShadow}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding FeedbackHistory}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource InputBackground}"
                                            CornerRadius="10"
                                            Padding="10"
                                            Margin="0,0,0,12"
                                            BorderBrush="{StaticResource BorderLight}"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="#FF7A00"
                                                                                  BlurRadius="20"
                                                                                  ShadowDepth="0"
                                                                                  Opacity="0.4"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Width="80" Height="80"
                                                    CornerRadius="8"
                                                    ClipToBounds="True"
                                                    Margin="0,0,12,0">
                                                <Image Source="{Binding Food.ImageUrl}"
                                                       Stretch="UniformToFill"
                                                       VerticalAlignment="Center"
                                                       HorizontalAlignment="Center"/>
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Food.Name}"
                                                           FontSize="15"
                                                           FontWeight="Bold"
                                                           Foreground="{StaticResource AccentBrush}"/>

                                                <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                                                           Foreground="{StaticResource TextSecondary}"
                                                           FontStyle="Italic"
                                                           FontSize="12"
                                                           Margin="0,2,0,4"/>

                                                <ItemsControl ItemsSource="{Binding RatingStars}" Margin="0,0,0,6">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel Orientation="Horizontal"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Path Data="M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.63L12,2L9.19,8.63L2,9.24L7.46,13.97L5.82,21L12,17.27Z"
                                                                  Width="18" Height="18"
                                                                  Fill="{Binding}"
                                                                  Margin="1,0,1,0"
                                                                  Stretch="Uniform"/>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>

                                                <TextBlock Text="{Binding Comment}"
                                                           TextWrapping="Wrap"
                                                           Foreground="{StaticResource TextPrimary}"
                                                           FontSize="13"
                                                           Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>