<Window x:Class="RestaurantWPF.Views.Staff.Dialogs.TableSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:RestaurantWPF.Converters"
        mc:Ignorable="d"
        Title="Chọn bàn" Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize"
        FontFamily="Segoe UI">

    <Window.Resources>
        <conv:TableStatusTextConverter x:Key="TableStatusTextConverter"/>
        <conv:TableStatusBrushConverter x:Key="TableStatusBrushConverter"/>

        <SolidColorBrush x:Key="AppBackground" Color="#F2F3F5"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrushLight" Color="#EAEAEA"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#222222"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#555555"/>
        <SolidColorBrush x:Key="StatusPendingBrush" Color="#FF9E40"/>
        <SolidColorBrush x:Key="StatusInProgressBrush" Color="#007BFF"/>
        <SolidColorBrush x:Key="StatusReadyBrush" Color="#28A745"/>
        <SolidColorBrush x:Key="StatusCancelledBrush" Color="#DC3545"/>
        <SolidColorBrush x:Key="RowSelectedBackground" Color="#E3F2FD"/>
        <SolidColorBrush x:Key="RowHoverBackground" Color="#F1F1F1"/>

        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="15"
                          ShadowDepth="2"
                          Opacity="0.08"
                          Color="#000000"/>
        <DropShadowEffect x:Key="SelectedShadow"
                          BlurRadius="20"
                          ShadowDepth="0"
                          Opacity="0.5"
                          Color="#FF7A00"/>

        <Style x:Key="TableToggleCardStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="100"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="CardBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{Binding Status, Converter={StaticResource TableStatusBrushConverter}}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Effect="{StaticResource CardShadow}">
                            <ContentPresenter Margin="12,10"
                                              VerticalAlignment="Top"
                                              HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="{StaticResource RowSelectedBackground}"/>
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="{StaticResource StatusPendingBrush}"/>
                                <Setter TargetName="CardBorder" Property="Effect" Value="{StaticResource SelectedShadow}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="{StaticResource RowHoverBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BaseActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ConfirmActionButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="{StaticResource StatusReadyBrush}"/>
        </Style>
        <Style x:Key="CancelActionButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="{StaticResource StatusCancelledBrush}"/>
        </Style>

    </Window.Resources>

    <Border CornerRadius="12" 
            Background="{StaticResource AppBackground}" 
            BorderBrush="{StaticResource BorderBrushLight}" 
            BorderThickness="1"
            Effect="{StaticResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Chọn bàn cho đơn hàng"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{StaticResource StatusPendingBrush}"
                       Margin="20"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Tables}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Style="{StaticResource TableToggleCardStyle}"
                                          IsChecked="{Binding IsSelected, Mode=TwoWay}">

                                <StackPanel>
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="18"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource TextPrimary}"/>
                                    <TextBlock Margin="0,5,0,0" Foreground="{StaticResource TextSecondary}">
                                        <Run Text="Sức chứa:"/>
                                        <Run Text="{Binding Capacity}" FontWeight="SemiBold"/>
                                    </TextBlock>

                                    <TextBlock Margin="0,5,0,0"
                                               Text="{Binding Status, Converter={StaticResource TableStatusTextConverter}}"
                                               Foreground="{Binding Status, Converter={StaticResource TableStatusBrushConverter}}"
                                               FontWeight="Bold"/>
                                </StackPanel>
                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Border Grid.Row="2" 
                    Background="{StaticResource CardBackground}" 
                    BorderThickness="0,1,0,0"
                    BorderBrush="{StaticResource BorderBrushLight}"
                    Padding="15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Hủy"
                            Style="{StaticResource CancelActionButtonStyle}"
                            Command="{Binding CancelCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

                    <Button Content="Xác nhận"
                            Style="{StaticResource ConfirmActionButtonStyle}"
                            Command="{Binding ConfirmCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>