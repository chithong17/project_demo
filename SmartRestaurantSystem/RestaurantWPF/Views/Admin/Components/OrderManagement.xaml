<UserControl x:Class="RestaurantWPF.Views.Admin.Components.OrderManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:RestaurantWPF.ViewModels.Admin">


    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 🔹 Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Tiêu đề -->
            <TextBlock Text="📦 Order Management"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       VerticalAlignment="Center"
                       Grid.Column="0"/>

            <!-- Nút chức năng -->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">

                <Button Content="➕ Add"
                        Command="{Binding AddCommand}"
                        Margin="10,0"
                        Width="90"
                        Background="#27AE60"
                        Foreground="White"/>

                <Button Content="✏️ Edit"
                        Command="{Binding EditCommand}"
                        Margin="10,0"
                        Width="90"
                        Background="#2980B9"
                        Foreground="White"/>

                <Button Content="🗑️ Delete"
                        Command="{Binding DeleteCommand}"
                        Margin="10,0"
                        Width="90"
                        Background="#C0392B"
                        Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- 🔹 Bảng danh sách -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Orders}"
                  SelectedItem="{Binding SelectedOrder}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  RowHeight="40"
                  AlternatingRowBackground="#F9FAFB">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding OrderId}" Width="Auto"/>
                <DataGridTextColumn Header="Table" Binding="{Binding Table.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Customer" Binding="{Binding Customer.FullName }" Width="*"/>
                <DataGridTextColumn Header="Staff" Binding="{Binding Staff.FullName }" Width="*"/>
                <DataGridTextColumn Header="Time" Binding="{Binding OrderTime}" Width="Auto"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto"/>
                <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="2*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>


            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
