<UserControl x:Class="RestaurantWPF.Views.Admin.Components.FoodManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🍽️ Food Management"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,15"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Add"
                    Width="90" Height="32"
                    Margin="0,0,10,0"
                    Background="#4CA1AF" Foreground="White" FontWeight="Bold"
                    Command="{Binding AddCommand}"/>
            <Button Content="Edit"
                    Width="90" Height="32"
                    Margin="0,0,10,0"
                    Background="#FFA726" Foreground="White" FontWeight="Bold"
                    Command="{Binding EditCommand}"/>
            <Button Content="Delete"
                    Width="90" Height="32"
                    Margin="0,0,10,0"
                    Background="#EF5350" Foreground="White" FontWeight="Bold"
                    Command="{Binding DeleteCommand}"/>
            <Button Content="Refresh"
                    Width="90" Height="32"
                    Background="#78909C" Foreground="White" FontWeight="Bold"
                    Command="{Binding LoadCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Foods}"
                  SelectedItem="{Binding SelectedFood, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True" RowHeight="40"
                  HeadersVisibility="Column"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding FoodId}" Width="60"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Price" Width="120">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Price" StringFormat="{}{0:N0} đ" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="300">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="TextBlock.TextTrimming" Value="CharacterEllipsis"/>
                            <Setter Property="ToolTip" Value="{Binding Description}"/>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="150"/>
                <DataGridCheckBoxColumn Header="Available" Binding="{Binding IsAvailable}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>