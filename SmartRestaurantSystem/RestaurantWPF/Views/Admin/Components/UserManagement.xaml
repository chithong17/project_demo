<UserControl x:Class="RestaurantWPF.Views.Admin.Components.UserManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:RestaurantWPF.ViewModels.Admin">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 🔹 Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="👥 User Management"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="➕ Add"
                        Command="{Binding AddCommand}"
                        Width="90"
                        Margin="10,0"
                        Background="#27AE60"
                        Foreground="White"/>

                <Button Content="✏️ Edit"
                        Command="{Binding EditCommand}"
                        Width="90"
                        Margin="10,0"
                        Background="#2980B9"
                        Foreground="White"/>

                <Button Content="🗑️ Delete"
                        Command="{Binding DeleteCommand}"
                        Width="90"
                        Margin="10,0"
                        Background="#C0392B"
                        Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- 🔹 Danh sách user -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  RowHeight="40"
                  AlternatingRowBackground="#F9FAFB">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="Auto"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="Auto"/>
                <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
