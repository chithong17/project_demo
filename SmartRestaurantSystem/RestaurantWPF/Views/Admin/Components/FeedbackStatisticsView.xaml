<UserControl x:Class="RestaurantWPF.Views.Admin.Components.FeedbackStatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:RestaurantWPF.Converters"
             mc:Ignorable="d"
             Background="#F2F3F5"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <FontFamily x:Key="IconFont">Segoe MDL2 Assets</FontFamily>

        <SolidColorBrush x:Key="AppBackground" Color="#F2F3F5"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrushLight" Color="#EAEAEA"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#222222"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#555555"/>
        <SolidColorBrush x:Key="TextMuted" Color="#888888"/>
        <SolidColorBrush x:Key="ControlBarBackground" Color="#FAFAFA"/>
        <SolidColorBrush x:Key="StatusPendingBrush" Color="#FF9E40"/>
        <SolidColorBrush x:Key="StatusInProgressBrush" Color="#007BFF"/>
        <SolidColorBrush x:Key="StatusReadyBrush" Color="#28A745"/>
        <SolidColorBrush x:Key="StatusCancelledBrush" Color="#DC3545"/>
        <SolidColorBrush x:Key="HeaderAccentBrush" Color="#1565C0"/>
        <SolidColorBrush x:Key="StarBrush" Color="#FFB700"/>

        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="15"
                          ShadowDepth="2"
                          Opacity="0.08"
                          Color="#000000"/>

        <Style x:Key="BaseActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="{StaticResource StatusInProgressBrush}"/>
        </Style>
        <Style x:Key="SecondaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="{StaticResource TextMuted}"/>
        </Style>

        <Style x:Key="FilterTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="34,6,8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="0" Focusable="False" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="&#xE721;" FontFamily="{StaticResource IconFont}" 
                                       VerticalAlignment="Center" HorizontalAlignment="Left"
                                       Foreground="{StaticResource TextSecondary}" FontSize="16" 
                                       Margin="10,0,0,0"
                                       IsHitTestVisible="False"/>
                            <TextBlock Text="Tìm theo món, khách..."
                                       Foreground="{StaticResource TextMuted}"
                                       VerticalAlignment="Center" HorizontalAlignment="Left"
                                       Margin="34,0,0,0"
                                       IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource StatusPendingBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style x:Key="FilterDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
        </Style>

    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📊 Thống kê phản hồi khách hàng"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="{StaticResource HeaderAccentBrush}"
                   Margin="0,0,0,20"/>

        <Border Grid.Row="1"
                Background="{StaticResource ControlBarBackground}"
                Padding="10,5"
                CornerRadius="10"
                BorderBrush="{StaticResource BorderBrushLight}"
                BorderThickness="1"
                Effect="{StaticResource CardShadow}">
            <StackPanel Orientation="Horizontal">

                <TextBox Width="220" 
                         Style="{StaticResource FilterTextBoxStyle}"
                         Margin="5"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                

                <DatePicker Width="150" 
                            Style="{StaticResource FilterDatePickerStyle}"
                            Margin="5"
                            SelectedDate="{Binding FromDate}"/>
                <DatePicker Width="150" 
                            Style="{StaticResource FilterDatePickerStyle}"
                            Margin="5"
                            SelectedDate="{Binding ToDate}"/>

                <Button Content="Áp dụng"
                        Command="{Binding ApplyFilterCommand}"
                        Style="{StaticResource PrimaryActionButtonStyle}"/>

                <Button Content="Xóa lọc"
                        Command="{Binding ClearFilterCommand}"
                        Style="{StaticResource SecondaryActionButtonStyle}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,20,0">

                <Border Background="White" CornerRadius="10"
                        Padding="20"
                        BorderBrush="{StaticResource BorderBrushLight}" BorderThickness="1"
                        Margin="0,0,0,20"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="⭐ Điểm trung bình:"
                                   FontSize="16" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondary}"/>
                        <TextBlock Text="{Binding AvgRating, StringFormat='{}{0:F1}'}"
                                   FontSize="36"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource StatusPendingBrush}"/>
                        <TextBlock Text="{Binding TotalFeedbacks, StringFormat='Tổng: {0} phản hồi'}"
                                   FontSize="14" Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10"
                        Padding="20"
                        BorderBrush="{StaticResource BorderBrushLight}" BorderThickness="1"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="📌 Biểu đồ đánh giá"
                                   FontSize="16" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimary}"
                                   Margin="0,0,0,10"/>

                        <ItemsControl ItemsSource="{Binding RatingGroups}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,5,0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock FontWeight="SemiBold" 
                                                   Foreground="{StaticResource TextSecondary}"
                                                   Grid.Column="0">
                                            <Run Text="{Binding Rating}"/>
                                            <Run Text="sao"/>
                                        </TextBlock>

                                        <ProgressBar Value="{Binding Count}"
                                                     Maximum="{Binding DataContext.MaxFeedbackCount, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                     Height="10"
                                                     Grid.Column="1"
                                                     Margin="10,0"
                                                     BorderThickness="0"
                                                     Background="{StaticResource BorderBrushLight}"
                                                     Foreground="{StaticResource StatusPendingBrush}"/>

                                        <TextBlock Text="{Binding Count}" 
                                                   FontWeight="SemiBold"
                                                   Grid.Column="2"
                                                   Foreground="{StaticResource TextSecondary}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Column="1"
                    Background="White"
                    CornerRadius="10"
                    BorderBrush="{StaticResource BorderBrushLight}"
                    BorderThickness="1"
                    Padding="15"
                    Effect="{StaticResource CardShadow}">

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding FilteredFeedbacks}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource CardBackground}"
                                        CornerRadius="8"
                                        BorderBrush="{StaticResource BorderBrushLight}"
                                        BorderThickness="1"
                                        Padding="15"
                                        Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <DockPanel Grid.Row="0">
                                            <TextBlock Text="{Binding Food.Name}"
                                                       FontSize="16"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource HeaderAccentBrush}"
                                                       DockPanel.Dock="Left"/>
                                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding Rating, StringFormat='⭐ {0}'}"
                                                           FontSize="16"
                                                           FontWeight="Bold"
                                                           Foreground="{StaticResource StatusPendingBrush}"/>
                                            </StackPanel>
                                        </DockPanel>

                                        <TextBlock Grid.Row="1" 
                                                   Foreground="{StaticResource TextSecondary}"
                                                   FontSize="12"
                                                   Margin="0,2,0,8">
                                            <Run Text="bởi"/>
                                            <Run Text="{Binding Customer.FullName}" FontWeight="SemiBold"/>
                                            <Run Text="vào ngày"/>
                                            <Run Text="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy}}"/>
                                        </TextBlock>

                                        <TextBlock Grid.Row="2"
                                                   Text="{Binding Comment}"
                                                   TextWrapping="Wrap"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   FontStyle="Italic"
                                                   FontSize="14"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>